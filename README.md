# Mini Ticket System

A simple support ticket app built with Plain PHP, MySQL, and Bootstrap 5.

## Setup

1. Import the database: `mysql -u root -p < miniticketsystem.sql`
   (Default admin login: username `admin`, password `admin123`)
2. Adjust the DB credentials in `config.php` if needed.